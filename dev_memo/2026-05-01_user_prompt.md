# ユーザープロンプト履歴

---

## 2026-05-01 — Web UI 追加依頼

uiを作ってもらっていいですか？ブラウザです。wbsファイルをアップロード（ローカル保存）、実行で処理をローカルに投げる、アウトルックへの反映。その逆の操作も要ります。実行ファイルはrun.batにしてください。design.mdを参考にしてください。

---

### 対応内容

- `app.py` — Flask Web サーバー（REST API + SSE ストリーミング）
- `templates/index.html` — ブラウザUI（タブ3画面、リアルタイムログ）
- `run.bat` — 起動バッチ（依存パッケージ確認＋ブラウザ自動オープン）
- `requirements.txt` に Flask を追加

### UI構成

| タブ | 内容 |
|---|---|
| 同期操作 | ファイル選択・AP_Mail入力・WBS↔Outlook実行・ログ表示・ダウンロード |
| ファイル管理 | ドラッグ&ドロップアップロード・一覧・ダウンロード・削除 |
| セットアップ | add-time-cols / add-status-col / new-wbs フォーム |
